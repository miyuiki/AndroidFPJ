<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jay.androidfpj">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Favorite" android:label="我的最愛"/>
        <activity android:name=".Account" android:label="帳號管理"/>
        <activity android:name=".Login" android:label="登入"/>
        <activity android:name=".Create" android:label="建立帳號"/>
        <activity android:name=".Search" android:label="搜尋" />
        <activity android:name=".Stock" android:label="個股資訊"/>
    </application>

</manifest>